import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    textStyle: {
        fontSize: 40,
        color: 'green',
        fontWeight: 'bold',
        marginTop: 100
    }
})

export default styles;